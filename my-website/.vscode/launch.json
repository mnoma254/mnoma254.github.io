{
    
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome against localhost",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080", // Ensure this matches the port used by Live Server
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}